//// ToolDefinition is a record with public fields. No logic to test.
//// Compiler enforces construction and equality.
//// Schema output tested implicitly via openai_test build_request_body tests.

import gleeunit

pub fn main() -> Nil {
  gleeunit.main()
}
